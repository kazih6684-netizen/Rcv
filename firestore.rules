rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /payments/{document=**} {
      allow read, write: if true;
    }
    match /failed_parse_logs/{document=**} {
      allow read, write: if true;
    }
  }
}
